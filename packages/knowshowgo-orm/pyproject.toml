[tool.poetry]
name = "knowshowgo-orm"
version = "0.0.1"
description = "KnowShowGo client/ORM (monorepo package)"
authors = ["KnowShowGo"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
requests = "^2.31"
pydantic = "^2.9.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
